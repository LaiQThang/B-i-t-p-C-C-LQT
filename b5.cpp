#include <stdio.h>
using namespace std;
int main()
{
	int a,b;
			printf("Xin moi nhap vao so thu nhat:");
		scanf("%d", &a);
			printf("Xin moi nhap vao so thu hai:");
		scanf("%d", &b);
	if (a>b) printf("So lon nhat la %d", a);
			else 
	if (a<b)
			printf("So lon nhat la %d", b);
return 0;
}

